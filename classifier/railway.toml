[build]
builder = "DOCKERFILE"
dockerfilePath = "classifier/Dockerfile"
watchPatterns = ["classifier/**", "issue-taxonomy.json"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
